apply plugin: 'java'
apply plugin: 'eclipse'

sourceSets {
  main {
     java.srcDirs = ['src']
     output.classesDir = 'out/classes'
      resources {
       srcDirs = ['src']
     }
   }
}

dependencies {
    compile fileTree(dir: "<%= libPath %>", include: ['*.jar'])
}

clean.doFirst {
	delete "${rootDir}/out"
}

jar {
	baseName '<%= interfaceJarName %>'
    destinationDir = file('out/lib')
}

defaultTasks 'clean', 'jar'
